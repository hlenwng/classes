#ifndef MEDIA_H
#define MEDIA_H
#include <iostream>
#include <cstring>
using namespace std;

class Media {
 public:
  Media();
  Media(char*t, int y);
  
  char* title;
  virtual char* getTitle();

  int year;
  virtual int getYear();

  virtual void printM();
};

#endif
